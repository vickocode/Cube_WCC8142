const input_number = 6;

function printCube(input_number) {
  for (let i = 1; i <= input_number; i++) {
    const cube = Math.pow(i, 3);
    console.log(`Current Number is: ${i} and the cube is asdsads: ${cube}`);
  }
}

printCube(input_number);
